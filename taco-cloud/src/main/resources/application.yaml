spring:
  application:
    name: tacocloud
  datasource:
    name: tacocloud
    generate-unique-name: false
  data:
    rest:
      base-path: /data-api
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:9001/oauth2/jwks